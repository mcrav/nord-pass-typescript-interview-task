export enum Routes {
  Login = '/login',
  PasswordHealth = '/items',
  Weak = '/items/weak',
  Reused = '/items/reused',
  Root = '/',
}

export enum API {
  Logout = 'api/logout',
  Login = 'api/login',
  Items = 'api/items',
  User = 'api/user',
}
